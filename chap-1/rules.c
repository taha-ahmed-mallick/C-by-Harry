#include<stdio.h>

int main() {
    int a;
    int A; //different
    int b;
    int _; // only underscore is the special character which can be used as
           // a variable name. Any other special not allowed
    int aB_C;
    // int a; this would also cause an error as a was already declared
    // const int a; this would cause an error as a was already declared and as a variable
    return 0;
}