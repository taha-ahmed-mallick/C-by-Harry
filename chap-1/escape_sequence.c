#include <stdio.h>

int main() {
    // \n -> NEW LINE
    // \t -> TAB
    // \" -> for "
    // \\ -> for \

    printf("Hello, this is a tab \t followed by a new line \nand if i want \"double qutoes\" also, \'single\'\nbackslash: \\");
    return 0;
}