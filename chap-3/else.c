#include <stdio.h>

int main() {
    int age = 5;
    if (age > 10)
    {
        printf("We are inside if.\n");
        printf("Your age is greater than 5.\n");
    } else {
        printf("You are a child.");
    }
    return 0;
}