#include <stdio.h>

int main() {
    int a = 5;
    float b = 9;
    printf("%f", a/b);
    // + -> to add
    // - -> to sub
    // * -> to mul
    // / -> to div
    // no exponentiation func (^ is used for different purpose)
    return 0;
}